import 'package:flutter/material.dart';

class ActividadesScreen extends StatelessWidget {

  const ActividadesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // Implementa tu lista completa aquí
    return Scaffold(
      appBar: AppBar(title: const Text("Actividades")),
      body: Center(child: Text("Lista de actividades para nada")),
    );
  }
}
